<div class="container mt-5">
  <div class="row">
    <div class="col-md-3">
     <%= render 'customer/items/genre-search' %>
    </div>
    <div class="col-md-9 mx-auto">
      <div class="jumbotron">
        <p class="h4">
          ようこそ、ながのcakeへ！<br>
          このサイトは、ケーキ販売のECサイトになります。<br>
          会員でない方も商品の閲覧はできますが、<br>
          購入には会員登録が必要になります。
        </p>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-md-3"></div>
    <div class="col-md-9">
      <h2 class="mb-5">新着商品</h2>
      <div class="d-flex flex-wrap">
        <% @items.each do |item| %>
          <div class="w-20 mx-auto mt-4">
            <%= link_to item_path(item) do %>
              <%= image_tag item.get_image(200,200)%>
            <% end %>
            <div>
              <%= link_to item_path(item) do %>
                <p class="text-dark mt-2"><%= item.name %></p>
              <% end %>
              <p>￥<%= item.price.to_s(:delimited) %></p>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-12 text-right">
      <%= link_to "すべての商品を見る>", items_path %>
    </div>
  </div>
</div>