<div class="col-sm-12 col-md-8 col-lg-4 px-5 px-sm-0">
  <table class="table">
    <thead bgcolor="#816F5B" class="text-light">
      <tr>
        <th>ジャンル</th>
        <th colspan="2"></th>
      </tr>
    </thead>
    <tbody>
      <% genres.each do |genre| %>
        <tr>
          <td><%= genre.name %></td>
          <td><%= link_to "編集する", edit_admin_genre_path(genre), class: "btn",style: "background-color: #816F5B; color: #fff;" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>